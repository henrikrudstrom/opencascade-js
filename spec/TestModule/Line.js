const TestModule = require('../../lib/TestModule/Line.js');

describe('Line', () => {
  it('Line(Ax1)', () => {
    // test 
  });

  it('Line(Pnt, Dir)', () => {
    // test 
  });

  it('setPosition(Ax1)', () => {
    // test 
  });

});